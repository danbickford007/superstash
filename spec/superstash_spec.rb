require "spec_helper"

describe Superstash do
  it "has a version number" do
    expect(Superstash::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
